#This configuration is pre-filled with the default values.
#You can remove keys you don't want to set. If a key is missing, the value set in the UI will be used; if that is also unset, the default value applies.

general:
  #On which URL Transfer is available
  appUrl: http://localhost:3000
  #Whether to set the secure flag on cookies. If enabled, the site will not function when accessed over HTTP.
  secureCookies: "false"
  #Time after which a user must log in again (default: 3 months).
  sessionDuration: 3 months
  #This applies to all users, each user can still personalise their language in their profile.
  defaultLanguage: fr-FR
share:
  #Whether registration is allowed
  allowRegistration: "true"
  #Lets signed-out visitors create shares and makes the site public. Off, the site requires signing in for everything, including the home page.
  allowUnauthenticatedShares: "false"
  #Whether anonymous share creators must verify their email with a one-time code before uploading. Only applies when allowUnauthenticatedShares is enabled.
  requireEmailVerificationForAnonymousShares: "true"
  #Maximum share expiration. Set to 0 to allow unlimited expiration. Admins,
  #and any user granted the "permanent shares" permission, bypass this cap.
  maxExpiration: 30 days
  #The default expiration time selected when creating a new share.
  defaultExpiration: 3 days
  #Default length for the generated ID of a share. This value is also used to generate links for reverse shares. A value below 8 is not considered secure.
  shareIdLength: "8"
  #Maximum share size
  maxSize: "15000000000"
  #Adjust the level to balance between file size and compression speed. Valid values range from 0 to 9, with 0 being no compression and 9 being maximum compression. 
  zipCompressionLevel: "9"
  #Adjust the chunk size for your uploads to balance efficiency and reliability according to your internet connection. Smaller chunks can enhance success rates for unstable connections, while larger chunks make uploads faster for stable connections.
  chunkSize: "10000000"
  #The share creation modal automatically appears when a user selects files, eliminating the need to manually click the button.
  autoOpenShareModal: "false"
  #Force reverse shares to be created in simple mode. If disabled, the creator of the reverse share can choose between simple and advanced mode.
  reverseShareSimpleOnly: "false"
  #Allow administrators to access all shares, even if they are password protected, expired or deleted.
  allowAdminAccessAllShares: "false"
  #When enabled, shares sent to a registered user's email address will automatically appear in their account. Users can also restrict share access to named recipients only.
  enableUserRecipients: "false"
  #How long files are kept after a share expires or gets deleted. Only useful if the 'Allow admin access to all shares' is also enabled. Set to -1 to keep files forever.
  fileRetentionPeriod: 0 days
verification:
  #Subject of the email that contains the one-time verification code.
  codeSubject: Votre code de vérification
  #Message which gets sent with the verification code. {code} will be replaced with the actual code.
  codeMessage: >-
    Voici votre code de vérification : {code}


    Il expire dans 10 minutes. Saisissez-le pour continuer votre transfert.
clamav:
  #Scan every uploaded share with ClamAV before keeping it (needs a reachable ClamAV instance, see the integrations docs).
  enabled: "true"
cache:
  #Normally Transfer caches information in memory. If you run multiple instances of Transfer, you need to enable Redis caching to share the cache between the instances.
  redis-enabled: "false"
  #Url to connect to the Redis instance used for caching.
  redis-url: redis://redis:6379
  #Time in second to keep information inside the cache.
  ttl: "60"
  #Maximum number of items inside the cache.
  maxItems: "1000"
email:
  #If enabled, emails will be sent in HTML format. Ensure email templates are updated to use HTML.
  sendHtmlEmails: "false"
  #Whether to allow email sharing with recipients. This can only be enabled if SMTP is activated.
  enableShareEmailRecipients: "false"
  #Subject of the email which gets sent to the share recipients.
  shareRecipientsSubject: Des fichiers ont été partagés avec vous
  #Message which gets sent to the share recipients. The creator, link, and expiration already appear automatically at the top of the email — this message only needs to cover the rest. Available variables:
  # {creator} - The username of the creator of the share
  # {creatorEmail} - The email of the creator of the share
  # {shareUrl} - The URL of the share
  # {desc} - The description of the share
  # {expires} - The expiration date of the share
  # These variables will be replaced with the actual value.
  shareRecipientsMessage: "{desc}"
  #Subject of the email sent to an anonymous (unauthenticated) sender confirming their own transfer link, so it isn't lost if the completion screen is closed before they copy it.
  anonymousSenderLinkSubject: Votre lien de transfert
  #Message sent to an anonymous sender with their own transfer link. The link and expiration already appear automatically at the top of the email — this message only needs to cover the rest. Available variables:
  # {name} - " « <share name> »" (with a leading space) when the share was named, otherwise empty
  # {shareUrl} - The URL of the share
  # {expires} - The expiration date of the share
  anonymousSenderLinkMessage: "Gardez cet e-mail : c'est le seul moyen de retrouver ce lien si vous le perdez."
  #Subject of the sent email when someone created a share with your reverse share link.
  reverseShareSubject: Votre lien de dépôt a été utilisé
  #Message which gets sent when someone created a share with your reverse share link. {shareUrl} will be replaced with the creator's name and the share URL.
  reverseShareMessage: "Un partage vient d'être créé avec votre lien de dépôt : {shareUrl}"
  #Subject of the sent email when a user requests a password reset.
  resetPasswordSubject: Réinitialisation du mot de passe
  #Message which gets sent when a user requests a password reset. {url} will be replaced with the reset password URL.
  resetPasswordMessage: >-
    Vous avez demandé une réinitialisation de mot de passe. Cliquez sur ce
    lien pour réinitialiser votre mot de passe : {url}

    Ce lien expire dans une heure.
  #Subject of the sent email when an admin invites a user.
  inviteSubject: Vous avez été invité(e)
  #Message which gets sent when an admin invites a user. {url} will be replaced with the invite URL, {email} with the email and {password} with the users password.
  inviteMessage: >-
    Vous avez été invité(e). Cliquez sur ce lien pour accepter l'invitation :
    {url}


    Vous pouvez utiliser l'adresse e-mail « {email} » et le mot de passe
    « {password} » pour vous connecter.
  #Whether to send an email to the share's creator or sender when a file is downloaded. Requires SMTP.
  enableShareDownloadNotifications: "false"
  #Whether to set the Reply-To header to the email address of the user who created the share.
  shareRecipientsReplyToCreator: "false"
  #Subject of the email which gets sent to the share creator when a named recipient downloads a file (Email mode).
  shareDownloadNotificationSubject: Votre fichier a été téléchargé
  #Message which gets sent to the share creator when a named recipient downloads a file (Email mode). Available variables:
  # {recipientEmail} - The email of the recipient
  # {fileName} - The downloaded file name
  # {shareUrl} - The URL of the share
  shareDownloadNotificationMessage: "{recipientEmail} a téléchargé {fileName} depuis votre partage : {shareUrl}"
  #Subject of the email sent to the creator or sender of a share with no named recipient (Link mode) when a file is downloaded.
  ownerDownloadNotificationSubject: Votre fichier a été téléchargé
  #Message sent to the creator or sender of a share with no named recipient (Link mode) when a file is downloaded. Available variables:
  # {fileName} - The downloaded file name
  # {shareUrl} - The URL of the share
  ownerDownloadNotificationMessage: "{fileName} a été téléchargé depuis votre partage : {shareUrl}"
  #Whether to require users to verify their email address before being able to sign in. This can only be enabled if SMTP is activated.
  enableEmailVerification: "false"
  #Subject of the email which gets sent to the user when they sign up.
  verificationSubject: Vérifiez votre compte
  #Message which gets sent to the user when they sign up. {code} will be replaced with the 6-digit code to type in, {url} with the equivalent clickable link — both lead to the same verification.
  verificationMessage: >-
    Vous venez de vous inscrire. Saisissez le code {code} pour vérifier votre
    compte, ou cliquez simplement sur ce lien : {url}


    Ce code expire dans 60 minutes.
smtp:
  #Whether SMTP is enabled. Only set this to true if you entered the host, port, email, user and password of your SMTP server.
  enabled: "false"
  #Only set this to true if you need to trust self signed certificates.
  allowUnauthorizedCertificates: "false"
  #Host of the SMTP server
  host: ""
  #Port of the SMTP server
  port: "0"
  #Email address from which the emails get sent
  email: ""
  #Username of the SMTP server
  username: ""
  #Password of the SMTP server
  password: ""
ldap:
  #Use LDAP authentication for user login
  enabled: "false"
  #URL of the LDAP server
  url: ""
  #Default user used to perform the user search
  bindDn: ""
  #Password used to perform the user search
  bindPassword: ""
  #Base location, where the user search will be performed
  searchBase: ""
  #The user query will be used to search the 'User base' for the LDAP user. %username% can be used as the placeholder for the user given input.
  searchQuery: ""
  #Group required for administrative access.
  adminGroups: ""
  #LDAP attribute name for the groups, an user is a member of. This is used when checking for the admin group.
  fieldNameMemberOf: memberOf
  #LDAP attribute name for the email of an user.
  fieldNameEmail: userPrincipalName
oauth:
  #Allow users to register via social login
  allowRegistration: "true"
  #Whether to ignore TOTP when user using social login
  ignoreTotp: "true"
  #Whether to disable password login
  #Make sure that an OAuth provider is properly configured before activating this configuration to avoid being locked out.
  disablePassword: "false"
  #Whether GitHub login is enabled
  github-enabled: "false"
  #Client ID of the GitHub OAuth app
  github-clientId: ""
  #Client secret of the GitHub OAuth app
  github-clientSecret: ""
  #Whether Google login is enabled
  google-enabled: "false"
  #Client ID of the Google OAuth app
  google-clientId: ""
  #Client secret of the Google OAuth app
  google-clientSecret: ""
  #Whether Microsoft login is enabled
  microsoft-enabled: "false"
  #Tenant ID of the Microsoft OAuth app
  #common: Users with both a personal Microsoft account and a work or school account from Microsoft Entra ID can sign in to the application. organizations: Only users with work or school accounts from Microsoft Entra ID can sign in to the application.
  #consumers: Only users with a personal Microsoft account can sign in to the application.
  #domain name of the Microsoft Entra tenant or the tenant ID in GUID format: Only users from a specific Microsoft Entra tenant (directory members with a work or school account or directory guests with a personal Microsoft account) can sign in to the application.
  microsoft-tenant: common
  #Client ID of the Microsoft OAuth app
  microsoft-clientId: ""
  #Client secret of the Microsoft OAuth app
  microsoft-clientSecret: ""
  #Whether Discord login is enabled
  discord-enabled: "false"
  #Limit signing in to users in a specific server. Leave it blank to disable.
  discord-limitedGuild: ""
  #Limit signing in to specific users by their Discord ID. Leave it blank to disable.
  discord-limitedUsers: ""
  #Client ID of the Discord OAuth app
  discord-clientId: ""
  #Client secret of the Discord OAuth app
  discord-clientSecret: ""
  #Whether OpenID Connect login is enabled
  oidc-enabled: "false"
  #Discovery URI of the OpenID Connect OAuth app
  oidc-discoveryUri: ""
  #Whether the “Sign out” button will sign out from the OpenID Connect provider
  oidc-signOut: "false"
  #Scopes which should be requested from the OpenID Connect provider.
  oidc-scope: openid email profile
  #Username claim in OpenID Connect ID token. Leave it blank if you don't know what this config is.
  oidc-usernameClaim: ""
  #Must be a valid JMES path referencing an array of roles. Managing access rights using OpenID Connect roles is only recommended if no other identity provider is configured and password login is disabled. Leave it blank if you don't know what this config is.
  oidc-rolePath: ""
  #Role required for general access. Must be present in a user’s roles for them to log in. Leave it blank if you don't know what this config is.
  oidc-roleGeneralAccess: ""
  #Role required for administrative access. Must be present in a user’s roles for them to access the admin panel. Leave it blank if you don't know what this config is.
  oidc-roleAdminAccess: ""
  #Client ID of the OpenID Connect OAuth app
  oidc-clientId: ""
  #Client secret of the OpenID Connect OAuth app
  oidc-clientSecret: ""
s3:
  #Whether S3 should be used to store the shared files instead of the local file system. WARNING: If ClamAV is active, files will be temporarily downloaded from S3 to be checked.
  enabled: "false"
  #The URL of the S3 bucket.
  endpoint: ""
  #The region of the S3 bucket.
  region: ""
  #The name of the S3 bucket.
  bucketName: ""
  #The default path which should be used to store the files in the S3 bucket.
  bucketPath: ""
  #The key which allows you to access the S3 bucket.
  key: ""
  #The secret which allows you to access the S3 bucket.
  secret: ""
  #Turn off for backends that do not support checksum (e.g. B2).
  useChecksum: "true"
legal:
  #Whether to show a link to imprint and privacy policy in the footer.
  enabled: "false"
  #The text which should be shown in the imprint. Supports Markdown. Leave blank to link to an external imprint page.
  imprintText: ""
  #If you already have an imprint page you can link it here instead of using the text field.
  imprintUrl: ""
  #The text which should be shown in the privacy policy. Supports Markdown. Leave blank to link to an external privacy policy page.
  privacyPolicyText: ""
  #If you already have a privacy policy page you can link it here instead of using the text field.
  privacyPolicyUrl: ""
#This configuration is used to create the initial user when the application is started for the first time.
#Make sure to change at least the password as soon as you log in!
initUser:
  enabled: false
  username: admin
  email: admin@example.com
  password: my-secure-password
  isAdmin: true
  ldapDN: ""
